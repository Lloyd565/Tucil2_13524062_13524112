#pragma once
#include <bits/stdc++.h>
#include "Point.hpp"
using namespace std;

class Parser {
    public:
        vector<Point> vertices;
        vector<int> faces;
        void parse(const std::string& filepath) {   
        }
};